# Stock-Trading-Matching-System-Client-Server-
This Project implements a client-server stock trading matching engine written entirely in C, using sockets, queues and multi-threaded handling. It simultates a simplified stock exchange where users can: Place buy and sell orders, use trigger conditions (±5% price rules), Cancel existing orders, submit bulk orders from file,retrieve completed orders
